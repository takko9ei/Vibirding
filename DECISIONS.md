# DECISIONS — 取舍记录

> 每个取舍记三行：**决定 / 为什么 / 代价或取舍**。面试逐字稿用。

## v2：Web 使用两页顶部导航的响应式 Neo Brutalism
- **决定**：React 只提供“记一笔”和“观察记录”两个页面；PC 使用顶部导航和双栏工作台，移动端沿用同一组件与视觉 token 纵向重排。详情、编辑、删除确认和写入结果均作为页内状态，不新增路由。
- **为什么**：当前产品只有输入确认与记录管理两类核心任务，常驻侧栏或额外 Dashboard 会浪费空间并增加导航成本；统一组件的响应式重排能让粗边框、硬阴影和鲜明色彩在 PC/手机上保持一致，同时避免维护两套前端。
- **代价/取舍**：宽屏信息密度和手机操作顺序需要分别调校，至少维护 1024/736/360px 三档验收；未来若加入统计、地图或账号设置，再重新评估导航结构，v2 不预留空页面。

## v2：未匹配照片按物种合并为待确认草稿
- **决定**：仅对已解析到唯一 `species_id` 的 unmatched 照片自动建草稿；同种多图合并，公共地点/日期/时段只有在全部文本草稿一致时才继承，自动草稿始终要求用户确认。
- **为什么**：照片中出现文字未提及的鸟也应成为可记录内容，但不能把名称歧义或不一致上下文变成系统猜测；先生成预览草稿仍然遵守“确认后才写库”。
- **代价/取舍**：无法识别、无法映射和歧义照片不会自动成记录，需要用户在前端补充；多图合并后的 observation 不保存单一聚合置信度，每张照片自己的识别置信度仍由照片元数据保存。

## v2：批量确认写入使用外层事务 + 逐条 savepoint
- **决定**：确认请求用一个外层事务创建 session/认领媒体，每条 observation 在 `begin_nested()` savepoint 中写入；可预期的单条失败进入 `failed[]`，其他条继续，最终 session 标 completed/partial/failed。
- **为什么**：产品规则要求部分成功，同时 session 和照片归属又必须整体一致；savepoint 能回滚一条而不撤销同批已成功条目，外层事务仍能在系统异常时全部回滚。
- **代价/取舍**：结果和事务控制比“一次 bulk insert”复杂，批内照片冲突按草稿顺序由前一成功项认领；失败 session 仍会保留用于审计。

## v2：服务层要求显式 confirmed，并锁定照片后再认领
- **决定**：`BatchWriteService.confirm()` 只有在 `confirmed is True` 时才打开事务；用 `FOR UPDATE` 锁定全部 media ID，未知或已被其他 session 使用时整批拒绝。
- **为什么**：未来 UI 按钮不是安全边界，服务本身必须阻止未确认写入；行锁避免两个并发确认把同一张照片关联到不同记录。
- **代价/取舍**：照片当前只能归属一个确认批次；重用同一媒体重新提交会被拒绝，如未来需要多对多复用应单独引入关联表。

## v2：物种名录采用 eBird Taxonomy + speciesCode
- **决定**：从 eBird Taxonomy 当前版本导入 `category=species`，简体中文 locale 使用 `zh_SIM`；`taxonomy_source="ebird"`、`taxonomy_key=speciesCode`，科学名作为照片候选到名录的首选解析字段。
- **为什么**：eBird/Clements 是全球统一且按年维护的鸟类名录，eBird 同时提供稳定代码、科学名和多语言常用名；项目已有 eBird key 与适配基础，不必再维护第二套外部系统。
- **代价/取舍**：名录年度更新时名称、拆分和合并仍需同步；API 导入需要 eBird key。2.3 只做幂等更新，不自动猜测跨年度拆并关系。

## v2：名称只做分级精确解析，配对保持 dry-run
- **决定**：解析按“科学名 → 规范中文名 → 别名”逐级精确匹配，归一化只处理 Unicode、大小写和空白；多命中显式 ambiguous。图文配对只返回计划，不修改草稿或写 observations。
- **为什么**：物种匹配错误会污染长期数据；稳定 UUID 和可解释失败比模糊字符串相似度更安全。dry-run 让最易错的匹配逻辑能独立检查，再接确认写入。
- **代价/取舍**：俗名未登记时会 unmapped，需要维护 aliases 或由用户确认；2.3 暂不自动创建未匹配照片记录，那属于 2.5。

## v2：懂鸟适配器增加结构化结果，批处理逐图隔离失败
- **决定**：保留 `bird_id.run()` 的 v1 文本输出，同时增加 `identify()` 结构化接口；照片预处理按输入顺序逐张调用，每张输出 identified / unrecognized / failed，自动候选只取第一目标的第一项。
- **为什么**：后续 taxonomy 和匹配需要可靠字段，不能从给模型看的中文句子反向解析；逐图保留结果能让一张坏图不拖垮整批，并落实已拍板的“第一候选”规则。
- **代价/取舍**：适配器多维护一层结构化模型，批处理当前顺序执行、总耗时随照片数增长；本切片优先简单、可控和不冲击外部 API，不提前引入并发策略。

## v2：文本拆分用结构化返回工具 + 共享上下文信封
- **决定**：2.1 用一次 provider-neutral 模型调用；模型通过 `return_text_split` 返回 `shared_context + observations[]`，服务端负责共享字段下发、草稿编号和 pydantic 校验。
- **为什么**：共享上下文只抽取一次可减少重复和漂移，服务端合并能用确定性离线测试证明每条草稿都拿到地点/日期/时段；返回工具沿用现有手动函数调用边界，无需解析自由文本 JSON。
- **代价/取舍**：拆分质量仍取决于模型，且不合规响应会整批失败；本切片选择 fail-fast，不在结构错误时猜测或静默保留部分草稿。

## v2：PostgreSQL 使用 SQLAlchemy 2.x + Alembic + psycopg 3
- **决定**：同步数据访问使用 SQLAlchemy 2.x ORM，驱动使用 psycopg 3，schema migration 统一由 Alembic 管理；`observations.sequence_no` 使用数据库 identity 保证兼容查询能按插入顺序返回。
- **为什么**：这套组合直接满足既定的 PostgreSQL、ORM、显式 migration 和 FastAPI 后续接入要求；identity 顺序不依赖时间戳或 UUID，能准确保留 v1 `Log.query()` 的可观察顺序。
- **代价/取舍**：本地开发和离线 eval 都需要可用的 PostgreSQL；测试必须做 schema 隔离并清理，应用不能以 `create_all()` 偷代正式 migration。

## v2：物种名规范化采用名录 + 内部 species_id，不靠自由字符串匹配
- **决定**：建立 `species` 表；文本解析和照片鉴定的结果都映射到同一内部 `species_id`，图文匹配只按该 ID 进行。中文名只作为展示与检索字段，不作为匹配主键。
- **为什么**：v1 的 `Observation.species` 是自由字符串，用户俗名、模型推断和懂鸟正式名无法可靠做严格匹配；统一 ID 才能稳定支持多图归属、按种筛选和物种统计。
- **代价/取舍**：需要接入并维护物种名录、处理映射失败和别名；初期工作量更大，但不能用 prompt 自觉换取数据一致性。

## v2：媒体文件仅按内容哈希命名，不按物种名二次重命名
- **决定**：上传照片落到 `media/` 时以内容哈希作为文件名与去重键；不把物种名拼入或改写文件名。
- **为什么**：物种归属应由数据库外键表达；物种可能待确认、可被人工修正，而哈希文件名稳定、去重明确、不会让文件系统承担领域关系。
- **代价/取舍**：目录对人不再直观可读；需要在管理页或查询中通过数据库元数据展示原文件名、物种名和关联记录。

## v2：建立 sessions 表记录一次提交的批次关系
- **决定**：每次解析/确认提交对应一条 `sessions` 记录，关联其原始文本、上传媒体和最终创建的 observations。
- **为什么**：v2 的核心输入是一篇笔记拆成多条记录；没有批次实体就无法可靠回溯“这篇笔记产生了什么”、解释部分失败或支持后续重试。
- **代价/取舍**：数据库多一张表和关联逻辑；v1 单条 CLI 不需要它，因此只在 v2 数据库/批量流程中引入。

## read_log 只是"个人弱先验"，季节/分布核验另设权威工具
- **决定**：`read_log` 的职责收敛为"查我自己的历史观测"（个人记录 / 弱先验）；"某地某季是否合理出现某种鸟"这类权威核验，留给独立的鸟种分布工具 range_check（数据源 eBird，已升为正式切片 S3，见 §7/§10）。
- **为什么**：个人日志不代表生物地理事实——没记录过 ≠ 那儿没有，记录过也只是弱信号；权威物候/分布数据才是正主。v1 实测中 read_log 对季节判断几乎没提供权威价值，模型只能回退到自身知识。
- **代价/取舍**：read_log 的实际价值取决于日志攒了多少，起步阶段基本为空、鉴种主力是 bird_id + range_check；权威分布工具 range_check 已于 S3 落地接入 eBird（此条写下时尚未接，现已完成）；把多源"交叉合理性判断"暂压在主 agent 的 prompt 里，核验子 agent 仍作为后续进阶（§11）。

## range_check 用范式B（先取清单、模型再匹配），不用范式A（先猜种、工具核验单种）
- **决定**：`range_check(place, date)` 返回**该地近期实际记录的物种清单**，模型在清单内挑与外形描述匹配的种；而不是"模型先猜一个种、工具去回答这个种在不在"。
- **为什么**：清单把"开放式鸟类学知识题"收敛成"闭卷选择题"——模型只能从真实出现过的种里选，更准、更难凭空编造；eBird recent 一次就拿回全量，省往返。
- **代价/取舍**：清单可能很长（用 `back` 天 + 展示截断收窄）；"当季"用 recent 作代理，吃不了任意历史日期；清单外的罕见种会被漏（个人速记重常见种，可接受）。

## 地名→坐标用预存坐标表，不接地理编码 API
- **决定**：地名→经纬度用手维护的"常去观测点预存表"（`tools/locations.py`，按标准名 exact-match），命不中即降级（返回引导文本、不报错），不接 geocoding 服务。
- **为什么**：个人观鸟点就那么几个、低频新增；预存表零依赖、零成本、零延迟、坐标可控；接 geocoding 要多一个 key/网络/歧义消解，为个人级用量不划算。
- **代价/取舍**：命不中只能降级到纯知识推断；换城市/加新点要手动加一行；不具通用性（v1 不需要）。

## 运行时日期注入放在"入口层组装 messages"，不做成工具、也不改静态 prompt
- **决定**：`agent/prompt.py` 提供纯函数 `today_hint()`，由**入口脚本在组装 messages 时**拼进 system 内容（`SYSTEM_PROMPT + "\n\n" + today_hint()`）；`SYSTEM_PROMPT` 保持静态常量不变。全部 6 个真模型入口都接上；MockClient 入口（run_s1/check_s1）刻意不接。
- **为什么**：模型不知道"今天"，prompt 里"日期没写就用今天"的规则本来是空转（实测把年份都猜错）。做成工具要多一轮往返、纯属浪费；把日期写进静态 prompt 又会让常量随时间失真、且不可测。入口层注入最省、`now` 可注入所以可测。
- **代价/取舍**：每个入口都要记得拼这一行（曾一度只有 run_s6 接了、其余 5 个漏掉，造成"机制已落地但只覆盖 1/6"的假象）；将来若有统一 cli，应收敛成一处组装。

## S7 eval 用真 DeepSeek 跑，不用 MockClient；配额瓶颈是懂鸟而非 DeepSeek
- **决定**：`run_evals.py` 对每条用例跑**低温真 DeepSeek**（不用脚本化的 MockClient），但脚本内**限制调用次数**（每条小 `Budget(max_steps=…)` + 总用例数上限）；**带图用例严格限量**，用例集以无图为主。
- **为什么**：Mock 只能验证循环接线、验证不了模型真实的抽取/裁决/挑种能力，而 eval 的意义正是量化后者；DeepSeek API 很便宜，token 不是瓶颈。**真正稀缺的是懂鸟(hholove) API：只有 50 次免费调用**，而它是 `bird_id` 的后端。
- **代价/取舍**：eval 结果不再完全确定性（真模型有波动），通过率需按趋势看而非逐次比对；带图路径（裁决第3条）因配额只能薄测；离线确定性回归仍由 `scripts/check_s*.py` 承担。

## range_check 是主 agent 调的普通工具，不是核验子 agent
- **决定**：range_check 做成主 agent 直接调用的只读工具（注册→校验→执行→归一化），不拆成独立的"核验子 agent"。
- **为什么**：它的职责只是"取一份数据"——单步、无状态、不需要自己规划或多轮推理；工具契约已足够，子 agent 会凭空加编排/上下文复杂度，违背"砍到个人级"的右尺寸原则。
- **代价/取舍**：把 bird_id + range_check + read_log 的"二次合理性交叉判断"暂压在主 agent 的 prompt 里；真要做多源核验时再升级为子 agent（§11 进阶），现在不背这税。

## 手写 agent harness，不用 LangChain 等框架
- **决定**：循环 / 工具契约 / 权限闸 / 预算 / trace 以及 provider 适配全部手写（`agent/`、`tools/`、`harness/`、`llm/` 十来个小模块），不引入 LangChain/LlamaIndex 之类框架。
- **为什么**：这是"砍到个人级"的单 agent + 工具循环，逻辑就是 `model→tool→model` 一个回合——手写能把每一步看懂、可控、可测（132 条离线自检 + eval），也能逐行讲清"为什么这么设计"。框架会把这些藏进抽象层，带来版本耦合、隐藏的 prompt/重试行为，以及为通用性付的税，对个人级用量不划算。
- **代价/取舍**：工具注册、schema 校验、消息回填、重试等样板都得自己写一遍；换 provider 要自己写适配器——但因 provider 中立，只动 `llm/` 一个文件。

## 写入类操作过权限闸，且闸在执行路径内（不是事后补）
- **决定**：唯一的写工具 `append_log` 在真正执行前必须过 `permissions.check`（registry 里 `risk=="write"` 分支）；审批回调可注入（CLI 真人 y/n/a、`--yes`/eval 走自动策略）；读工具自动放行。
- **为什么**：写入是唯一不可逆、会改持久状态的操作，把闸放进**执行路径内**才能保证"未经同意绝不落盘"；可注入回调让同一套闸在交互、演示、测试三种场景复用（`input()` 只在入口层，绝不进 `permissions.check`）。
- **代价/取舍**：多一层确认，交互时要按键（`--yes` 可跳过）；无回调时 fail-closed（默认拒绝），可能挡住忘接 approver 的调用方——这是有意的保守。

## 记忆用 append-only JSONL，不用数据库
- **决定**：观测日志是一个只追加的 JSONL 文件（`memory/log.py`：`append` 只加行、`query` 顺序扫描过滤、坏行跳过），不上 SQLite/数据库。
- **为什么**：个人级数据量小、写少读少；JSONL 零依赖、人可读、可 `grep`、append-only 天然不改历史；"记忆就是一个只追加文件"也让它与 trace 同构、易讲。
- **代价/取舍**：查询是 O(n) 全扫、无索引；并发写无保护；数据量大了要换 SQLite（已登记为 §11 进阶，数据量大再说）。

## 手动函数调用，不用 SDK 的自动函数执行
- **决定**：只向模型声明 tools，工具由我们自己执行、结果作为 `role="tool"` 消息自己回填，不用任何 SDK 的自动函数执行。
- **为什么**：自己掌控"执行—回填"这一步，才能在中间插入权限闸、trace、预算、错误归一化——这些正是本项目的核心卖点；自动执行会把这段黑箱化、难观测、难加闸。
- **代价/取舍**：要自己写消息回填样板（assistant.tool_calls → 逐个 execute → role=tool 回填）；但这段样板恰恰是"可观测 + 可管控"的落点，值得。

## eval 题目/答案分文件防泄题；离线档据答案"合成理想模型"当回归保险
- **决定**：eval 用例拆成 `evals/tasks.yaml`（题目：input_note/image_path）与 `evals/answers.yaml`（答案：expected），按 id 配对；离线档不写死 mock 剧本，而是**据 answers 合成"理想模型"剧本**驱动真实循环。
- **为什么**：题目/答案分离杜绝任何路径把答案漏进喂给模型的 prompt（防泄题）；离线"据答案合成理想模型"让离线档成为**管线+比对器的回归保险**（应稳定 100%），与在线档"真 DeepSeek 测真实识别质量"分工明确、不重复造两套 mock 逻辑。
- **代价/取舍**：离线 100% 是构造出来的、测不出模型智能（那归在线档）；合成器要把 expected 翻成一份满足所有断言的写入参数，多一层间接。

## CLI 不设子命令，记录/查询由模型据"入口层意图前言"自判
- **决定**：`cli.py` 一个位置参数收笔记或问句，不做 `record`/`query` 子命令；在入口层把 `SYSTEM_PROMPT + today_hint() + 意图路由前言` 拼成 system 消息，让模型先判断"记录新观测"还是"查询历史"（查询只调 read_log、不写盘）。
- **为什么**：日常速记就是"随手丢一句"，不想先想"这是记录还是查询"再选子命令；意图判断本就是模型擅长的事。前言走入口层组装（与 `today_hint()` 同一手法），`SYSTEM_PROMPT` 常量与四个工具行为都不改。
- **代价/取舍**：路由靠模型，偶发误判（问句被当记录去写、或反之）无硬保证；靠前言 + `--yes` 时的写前摘要兜底，真要强约束可日后加显式 `--query` 开关。
